export function middleware() {
  // Middleware disabled temporarily due to Edge Runtime issues with Mongoose
  // TODO: Implement client-side route protection instead
}

export const config = {
  matcher: [],
};
